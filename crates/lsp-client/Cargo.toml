[package]
name = "lsp-client"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
tracing = "0.1"
tokio = { version = "1.35", features = ["full"] }
lsp-types = "0.94"

[dev-dependencies]
mockall = "0.12"
